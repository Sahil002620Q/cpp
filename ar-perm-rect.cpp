#include <iostream>
using namespace std;
int main () {
    int length,width;
    length = 4,width = 2;
    cout << "Area of rectangle is " << length*width << " sq unit and perimeter of rectangle is " << 2*(length+width) << " unit.";
}